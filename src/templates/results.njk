---
pagination:
  data: collections.results
  size: Infinity
  alias: results
title: Results
layout: base
permalink: "/results/"
---

<div class="main-inner__wrapper">

  <p>
    Click on a result below to view more details. If you'd like to add your result to the list and claim your reward, <a href="/contact/">send us your info</a>.
  </p>

  {% for year, yearResults in results | groupby('data.year') | dictsort | reverse %}
    <details open>
      <summary>
        {{ year }} Results
      </summary>
      <ul class="results-list">
      {% for result in yearResults | reverse %}
        <li class="results-list__item">
          <a href="/results/{{ result.data.run_date | slugify }}-{{ result.data.name | slugify }}/">
            <span>{{ result.data.run_date | shortDate }}</span>
            <span class="name">{{ result.data.name }}</span>
            <span>{{ result.data.run_duration }}</span>
          </a>
        </li>
      {% endfor %}
      </ul>
    </details>
  {% endfor %}

</div>

